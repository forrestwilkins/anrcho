<!-- manifestos -->

<% if @group %>
  <%= render "groups/manifesto_card", group: @group, hidden: false %>
<% else %>
  <%= render "manifestos/current", hidden: false %>
<% end %>

<% for manifesto in @proposed_manifestos %>
	<%= render "proposals/card", proposal: manifesto %>
<% end %>
