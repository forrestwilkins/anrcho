<!-- proposals -->

<% link_text = proposal.proposals.present? ? "Revisions #{proposal.proposals.size}" : "Revise" %>

<%= link_to link_text, show_proposal_path(token: proposal.unique_token, revisions: true) %>
