<!-- groups -->

<div align="center" id="new_group_form">
	<%= form_for @group do |f| %>
		<p>
			<%= f.text_area :body, placeholder: "Description (optional)" %>
		</p>
		<p>
			<%= f.label "Local" %>
			<%= check_box_tag :local %>
		</p>
		<%= f.submit "Create" %>
	<% end %>
</div>


