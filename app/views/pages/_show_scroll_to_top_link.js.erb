// pages

function show_scroll_to_top_link() {
  $(window).bindWithDelay("scroll", function() {
    if($(window).scrollTop() + $(window).height() > $(document).height() - 5) {
      $("#scroll_to_top_link").fadeIn();
    }
  }, 200);
}

show_scroll_to_top_link();
