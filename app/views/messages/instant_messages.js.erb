// messages

<% if @instant_messages.present? %>
  function load_instant_messages() {
    var anchor = $("#instant_messages_anchor");
	  anchor.after("<%=j render 'messages/instant_messages' %>");
	  anchor.remove(); $("#instant_messages_anchor").scrollView();
  }
  load_instant_messages();
<% end %>
