// messages

setInterval(function() {
	if ($("#instant_messages_anchor").length) {
		$.ajax({
			type: "GET",
			url: "/messages/instant_messages",
			data: $.param({token: "<%= @group.token %>"})
		});
	}
}, 2500);
