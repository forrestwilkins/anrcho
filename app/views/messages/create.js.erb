// messages

<% if @group or @receiver_token %>
  var form = $("#new_message_form");
  var anchor = $("#message_form_anchor");

  form.remove();
  anchor.after("<%=j render 'messages/new' %>");
<% end %>
